package com.chatforum.enums;

public class Enums {

    public enum MessageType {
        TEXT, IMAGE, VIDEO, FILE, LOCATION, EVENT, AUDIO
    }

    public enum AttachmentType {
        IMAGE, VIDEO, DOCUMENT, AUDIO, LOCATION, FILE, EVENT
    }
}
